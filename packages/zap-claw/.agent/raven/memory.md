# 🧠 MEMORY: Learned Project Facts

**Target System:** OLYMPUS

## 1. Core Operating Context

- This file stores data schema facts, query optimization patterns, and business metric baselines.
- All discovered performance bottlenecks, data anomalies, and their resolutions must be appended here.

## 2. Dynamic Memory Block

- (Agent will auto-append facts below this line during execution).

---
*Example: PostgreSQL `orders` table has an index on `(tenantId, created_at DESC)` — always include both in ORDER BY for optimal performance.*
