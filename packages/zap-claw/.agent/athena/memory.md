# 🧠 MEMORY: Learned Project Facts

**Target System:** OLYMPUS

## 1. Core Operating Context

- This file acts as the explicit, long-term memory store for Athena.
- Facts gathered during research sessions, SOP scans, or web intelligence missions must be appended here if they possess long-term strategic value.
- High-confidence facts (≥0.9) are auto-synced to `memory_world` MongoDB collection and vectorized into ChromaDB `zap-knowledge`.

## 2. Dynamic Memory Block

- (Agent will auto-append facts below this line during execution).

---
*Example: ChromaDB `zap-knowledge` collection contains embeddings for all SOPs from SOP-001 to SOP-036.*
